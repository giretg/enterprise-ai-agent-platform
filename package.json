{
  "name": "enterprise-ai-agent-platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix app",
    "build": "npm ci --prefix app && npm run build --prefix app",
    "start": "npm run start --prefix app",
    "db:push": "npm run db:push --prefix app",
    "db:seed": "npm run db:seed --prefix app",
    "db:backfill-audit": "npm run db:backfill-audit --prefix app",
    "test:acceptance": "npm run test:acceptance --prefix app",
    "deploy": "npx -y firebase-tools@latest deploy --only apphosting"
  },
  "engines": {
    "node": ">=20"
  }
}
