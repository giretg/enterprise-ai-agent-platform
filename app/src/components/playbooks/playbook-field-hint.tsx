/** Üzleti magyarázat mezők alatt a playbook-szerkesztőben. */
export function PlaybookFieldHint({ children }: { children: React.ReactNode }) {
  return <p className="mt-1 text-[11px] leading-snug text-ink-faint">{children}</p>
}
