/** Phase 0 stub — the 2700-line registry lives in legacy/. */
export type ToolName = string
export type DocsEditOperation = Record<string, unknown>
export type SlidesEditOperation = Record<string, unknown>
